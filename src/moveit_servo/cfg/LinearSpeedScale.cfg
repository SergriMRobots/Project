#!/usr/bin/env python
PACKAGE = "moveit_servo"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add("linear_scale", double_t, 0, "Part from max linear speed",   1, 0,   1)

exit(gen.generate(PACKAGE, "moveit_servo", "LinearSpeedScale"))
